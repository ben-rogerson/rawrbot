name: Deploy Clay Journal

on:
  push:
    branches: [main]
    paths:
      - 'projects/clay-journal/**'

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: projects/clay-journal/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: projects/clay-journal
      - run: pnpm typecheck
        working-directory: projects/clay-journal

  deploy-api:
    needs: typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: projects/clay-journal/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: projects/clay-journal
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: projects/clay-journal/packages/api
          command: deploy

  deploy-web:
    needs: typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: projects/clay-journal/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: projects/clay-journal
      - run: pnpm --filter @clay-journal/web build
        working-directory: projects/clay-journal
        env:
          VITE_API_URL: https://clay-journal-api.ben-rogerson.workers.dev
      - uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: projects/clay-journal/packages/web
          command: pages deploy dist --project-name clay-journal-web
